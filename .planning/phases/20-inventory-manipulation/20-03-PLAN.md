---
phase: 20-inventory-manipulation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - Threa/Threa.Client/Components/Shared/CharacterDetailModal.razor
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "CharacterDetailInventory receives CharacterId parameter from CharacterDetailModal"
    - "CharacterDetailInventory receives TableId parameter from CharacterDetailModal"
    - "Inventory operations can execute with valid CharacterId (not 0)"
    - "Inventory operations can execute with valid TableId (not Guid.Empty)"
    - "CharacterUpdateMessage publishing uses correct CharacterId and TableId"
  artifacts:
    - path: "Threa/Threa.Client/Components/Shared/CharacterDetailModal.razor"
      provides: "Integration of CharacterDetailInventory with required parameters"
      contains: 'CharacterDetailInventory Character="character" CharacterId='
  key_links:
    - from: "CharacterDetailModal.razor"
      to: "CharacterDetailInventory"
      via: "Component parameter binding"
      pattern: '<CharacterDetailInventory.*CharacterId=.*TableId='
---

<objective>
Fix the CharacterDetailModal.razor integration to pass required CharacterId and TableId parameters to CharacterDetailInventory component.

Purpose: Close the critical integration gap preventing all inventory manipulation operations from functioning. The CharacterDetailInventory component is fully implemented with 805 lines of working code, but receives CharacterId=0 and TableId=Guid.Empty due to missing parameter bindings.

Output: Working inventory manipulation in the GM modal - all INVT requirements become functional.
</objective>

<execution_context>
@C:\Users\rocky\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\rocky\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/20-inventory-manipulation/20-VERIFICATION.md

# File to fix
@Threa/Threa.Client/Components/Shared/CharacterDetailModal.razor

# Pattern reference (Effects tab shows correct parameter binding)
# Lines 133-137 of CharacterDetailModal.razor show the correct pattern
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add missing CharacterId and TableId parameters to CharacterDetailInventory</name>
  <files>Threa/Threa.Client/Components/Shared/CharacterDetailModal.razor</files>
  <action>
Fix the CharacterDetailInventory component instantiation on line 141 to include required parameters.

**Current code (line 141):**
```razor
<CharacterDetailInventory Character="character" />
```

**Change to:**
```razor
<CharacterDetailInventory Character="character"
                         CharacterId="selectedCharacterId"
                         TableId="TableId" />
```

This matches the pattern used for CharacterDetailEffects (lines 135-137) and CharacterDetailGmActions (lines 123-127), which correctly pass CharacterId and TableId parameters.

The parameters are:
- `CharacterId` binds to `selectedCharacterId` (the currently selected character in the modal)
- `TableId` binds to `TableId` (the table Guid parameter of the modal)
  </action>
  <verify>
1. `dotnet build Threa/Threa.Client/Threa.Client.csproj` succeeds
2. Verify the fix with grep: `grep -A2 "CharacterDetailInventory" Threa/Threa.Client/Components/Shared/CharacterDetailModal.razor` shows CharacterId and TableId parameters
  </verify>
  <done>
CharacterDetailInventory receives CharacterId and TableId parameters enabling all inventory operations (add, remove, equip, unequip, quantity edit, currency edit) and CharacterUpdateMessage publishing.
  </done>
</task>

</tasks>

<verification>
1. Build succeeds: `dotnet build Threa/Threa.Client/Threa.Client.csproj`
2. CharacterDetailInventory component instantiation includes CharacterId and TableId parameters
3. Pattern matches Effects tab: both use `CharacterId="selectedCharacterId" TableId="TableId"`
</verification>

<success_criteria>
- CharacterDetailInventory receives CharacterId parameter (not 0)
- CharacterDetailInventory receives TableId parameter (not Guid.Empty)
- All inventory operations can now execute with valid context
- CharacterUpdateMessage publishing will use correct campaign and character identifiers
</success_criteria>

<output>
After completion, create `.planning/phases/20-inventory-manipulation/20-03-SUMMARY.md`
</output>
